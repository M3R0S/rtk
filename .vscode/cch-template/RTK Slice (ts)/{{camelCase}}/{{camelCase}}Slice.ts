import { createSlice } from "@reduxjs/toolkit";

import { {{pascalCase}}State } from '.'

const initialState: {{pascalCase}}State = {

}

export const {{camelCase}}Slice = createSlice({
    name: "{{camelCase}}",
    initialState,
    reducers: {

    }
})

export const {  } = {{camelCase}}Slice.actions